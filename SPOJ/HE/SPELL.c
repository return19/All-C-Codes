#include<stdio.h>
#include<string.h>

char D[1000010][40];
char temp[40];
char a[10000];
int main()
{
    int d,i,j;
    scanf("%d",&d);

    for(i=0;i<d;i++)
    {
        scanf("%s",D[i]);

    }
    fflush(stdin);
    gets(a);
    puts(a);
    return 0;
}
